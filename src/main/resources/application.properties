# Ativa o perfil especificado ou usa 'dev' como valor padrão
spring.profiles.active=${APP_PROFILE:dev}
spring.jpa.open-in-view=false

# Configurações CORS
cors.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Configurações do PostgreSQL
# Usando SSL no banco de dados, mas você pode desabilitar se necessário (ssl=false)
spring.datasource.url=jdbc:postgresql://affectedly-justified-albatross.data-1.use1.tembo.io:5432/postgres-tembo?ssl=true
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=76qZNRE9UgZNr3hA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.datasource.hikari.maximum-pool-size=10

# Configurações do servidor
server.port=8080
